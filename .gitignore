# === Secrets & env ===
.env
.env.*       # .env.local, .env.production, dll
*.env

# Jangan ignore template contoh
!.env.example

# === Node / Express ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json # opsional: kalau kamu mau reproducible builds, HAPUS baris ini

# === Build & cache ===
dist/
build/
coverage/
.cache/
.tmp/
.temp/

# === Editor/OS ===
.vscode/
.vs/
.DS_Store
Thumbs.db

# === Certs & keys (kalau ada) ===
*.pem
*.key
*.crt
*.pfx

# === Uploads sementara (kalau berisi user data) ===
uploads/
